import { createFadeUpAnimation } from "../../utils/fadeUpAnimation";

export const ASSIGNMENT_HELP_CHAT_PHASE_START_PROGRESS = 0.25;

export const ASSIGNMENT_HELP_STAGE_SWAP_ANIMATION = createFadeUpAnimation({
  distancePx: 12,
  durationMs: 240,
});
