import { ClosingMessageSection } from "../common/closingMessageSection";

// Public section entry for the final closing block.
// 마지막 closing 블록의 외부 엔트리 섹션
export const WhatToDoSection = () => {
  return <ClosingMessageSection />;
};
