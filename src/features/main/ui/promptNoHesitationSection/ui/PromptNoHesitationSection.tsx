import { PromptAssemblySection } from "../common/promptAssemblySection";

// Public section entry for the prompt handoff block.
// 프롬프트 handoff 블록의 외부 엔트리 섹션
export const PromptNoHesitationSection = () => {
  return <PromptAssemblySection />;
};
